# Donation App Backend

This is the Flask backend for the Donation App.

## Features
- Donor & Admin login routes
- Fetch orphanages by category or all
- Add new orphanages
- Verify orphanages by admin
- Firebase Firestore database connection

## Setup
1. Upload your `serviceAccountKey.json` inside `/backend/utils`
2. Install dependencies:
